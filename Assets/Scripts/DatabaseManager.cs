using System;
using UnityEngine;

public class DatabaseManager : MonoBehaviour
{
    internal bool LoginUser(string username, string password)
    {
        throw new NotImplementedException();
    }

    private void Start()
    {
        Debug.Log("DatabaseManager is working!");
    }
}
